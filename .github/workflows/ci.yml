name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  quality:
    name: Lint, tipos y tests unitarios
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # Sin `version`: la toma del campo packageManager del package.json.
      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      # contracts se compila primero: api y web dependen de sus tipos.
      - run: pnpm --filter @bravo/contracts build

      # El cliente de Prisma no se versiona (depende del esquema, no al revés) y
      # typecheck y test lo necesitan para resolver sus tipos. No hace falta una
      # base de datos: generate solo lee el schema.
      - run: pnpm --filter @bravo/api prisma:generate

      - run: pnpm lint
      - run: pnpm format:check
      - run: pnpm typecheck
      - run: pnpm test
